# エージェント共通指示

このリポジトリで作業する AI エージェントは、利用する製品にかかわらず本ファイルを共通指示として扱う。

## 指示の参照

- 作業対象に対応するファイルが `.agents/instructions/` にある場合は、作業前にその内容を読み、適用する。
- Markdown ファイルを作成または編集する場合は、`.agents/instructions/markdown.md` を適用する。
- 複数の指示が該当する場合は、すべてを併用する。指示が競合する場合は、対象範囲が狭い指示を優先する。

## スキルの利用

- タスクに該当するスキルを `.agents/skills/` から選び、作業前に対象スキルの `SKILL.md` を読む。
- Simulink モデルの理解、作成、編集、シミュレーション、検証を行う場合は、`.agents/skills/using-simulink-agentic-toolkit/SKILL.md` を使用する。
- スキルはユーザーが指定した作業範囲と権限の内側で利用する。

## 作業方針

- 既存の設計、命名、ファイル構成を確認し、変更を必要な範囲に限定する。
- 変更後は、利用できる検証手段で結果を確認し、実施内容と未確認事項を簡潔に報告する。
- MATLAB または Simulink の実行環境が必要な作業では、利用可能な接続状態を確認してから実行する。
